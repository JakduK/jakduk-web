<!DOCTYPE html>
<!--[if IE 9]><html lang="{{locale}}" class="ie9" ng-app="jakdukApp"><![endif]-->
<!--[if !IE]><!--><html lang="{{locale}}" ng-app="jakdukApp"><!--<![endif]-->
{{#if baseHref}}
  <base href="{{baseHref}}">
{{/if}}
  <head>
  <title>{{#if preTitle}}{{{preTitle}}} &middot; {{/if}}{{{TITLE title}}}</title>
  {{> head_common}}
  {{#if headPage}}
    {{> (lookup . 'headPage')}}
  {{/if}}
  </head>

  <body class="{{bodyClass}}">
    {{{body}}}
  </body>
</html>
